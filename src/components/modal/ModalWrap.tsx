import styled from "styled-components";

const SModalWrap = styled.div`
  position: fixed;
  width: 1200dvw;
  height: 100dvh;
  rgba(0, 0, 0, 0.1)
`;

function ModalWrap() {
  return <SModalWrap></SModalWrap>;
}

export default ModalWrap;
